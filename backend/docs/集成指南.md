# LynShae 前后端联调指南

> 本文档指导开发者如何进行 Flutter 应用与 Node.js 后端 API 的集成联调

---

## 📋 目录

- [环境准备](#环境准备)
- [后端 API 配置](#后端 api 配置)
- [前端 API 服务](#前端 api 服务)
- [联调测试](#联调测试)
- [常见问题](#常见问题)

---

## 环境准备

### 后端环境

确保后端服务已启动并运行：

```bash
cd /Users/freakk/.nanobot/workspace/backend

# 方式一：使用测试服务器（开发环境）
node test-server-full.js

# 方式二：使用生产服务器
node src/server.js

# 方式三：使用 Docker（推荐生产环境）
docker-compose up -d
```

**验证后端服务**：
```bash
# 健康检查
curl http://localhost:3005/health

# 预期响应
# {"status":"ok","uptime":194.9s,"version":"1.0.0"}
```

### 前端环境

确保 Flutter 开发环境已配置：

```bash
cd /Users/freakk/Project/lynshae_app/app

# 获取依赖
flutter pub get

# 检查设备
flutter devices

# 运行应用
flutter run
```

---

## 后端 API 配置

### API 基础 URL

| 环境 | URL | 说明 |
|------|-----|------|
| **开发环境** | `http://localhost:3005/api/v1` | 本地测试服务器 |
| **生产环境** | `https://api.lynshae.com/api/v1` | 线上 API |
| **Docker 环境** | `http://host.docker.internal:3005/api/v1` | 容器内访问宿主机 |

### 配置环境变量

在 Flutter 项目中创建环境配置文件：

**文件**: `lib/config/api_config.dart`

```dart
class ApiConfig {
  // 开发环境
  static const String devBaseUrl = 'http://localhost:3005/api/v1';
  
  // 生产环境
  static const String prodBaseUrl = 'https://api.lynshae.com/api/v1';
  
  // 当前环境
  static const String currentEnv = 'development'; // 或 'production'
  
  // 获取当前环境的 Base URL
  static String get baseUrl {
    return currentEnv == 'production' ? prodBaseUrl : devBaseUrl;
  }
  
  // 连接超时时间（毫秒）
  static const int connectTimeout = 10000;
  static const int receiveTimeout = 10000;
}
```

---

## 前端 API 服务

### 创建 API 客户端

**文件**: `lib/services/api_client.dart`

```dart
import 'dart:convert';
import 'package:http/http.dart' as http;
import '../config/api_config.dart';
import 'auth_service.dart';

/// HTTP 响应包装类
class ApiResponse<T> {
  final String code;
  final String? message;
  final T? data;
  final bool success;

  ApiResponse({
    required this.code,
    this.message,
    this.data,
    required this.success,
  });

  factory ApiResponse.fromJson(
    Map<String, dynamic> json,
    T Function(Map<String, dynamic>)? fromJson,
  ) {
    return ApiResponse(
      code: json['code'] ?? 'INTERNAL_ERROR',
      message: json['message'],
      data: fromJson != null ? fromJson(json['data'] ?? {}) : json['data'] as T?,
      success: json['code'] == 'SUCCESS',
    );
  }
}

/// API 客户端 - 封装所有 HTTP 请求
class ApiClient {
  static final ApiClient _instance = ApiClient._internal();
  factory ApiClient() => _instance;
  ApiClient._internal();

  final String _baseUrl = ApiConfig.baseUrl;
  String? _accessToken;

  /// 设置 Access Token
  void setAccessToken(String? token) {
    _accessToken = token;
  }

  /// 获取请求头
  Map<String, String> _getHeaders({bool requiresAuth = false}) {
    final headers = {
      'Content-Type': 'application/json',
      'Accept': 'application/json',
    };
    if (requiresAuth && _accessToken != null) {
      headers['Authorization'] = 'Bearer $_accessToken';
    }
    return headers;
  }

  /// GET 请求
  Future<ApiResponse<T>> get<T>(
    String endpoint, {
    bool requiresAuth = false,
    Map<String, dynamic>? queryParams,
    T Function(Map<String, dynamic>)? fromJson,
  }) async {
    try {
      final uri = Uri.parse('$_baseUrl$endpoint').replace(
        queryParameters: queryParams,
      );
      final response = await http.get(
        uri,
        headers: _getHeaders(requiresAuth: requiresAuth),
      ).timeout(Duration(milliseconds: ApiConfig.connectTimeout));

      return _handleResponse<T>(response, fromJson);
    } catch (e) {
      return ApiResponse(
        code: 'NETWORK_ERROR',
        message: '网络请求失败：${e.toString()}',
        data: null,
        success: false,
      );
    }
  }

  /// POST 请求
  Future<ApiResponse<T>> post<T>(
    String endpoint, {
    Map<String, dynamic>? body,
    bool requiresAuth = false,
    T Function(Map<String, dynamic>)? fromJson,
  }) async {
    try {
      final response = await http.post(
        Uri.parse('$_baseUrl$endpoint'),
        headers: _getHeaders(requiresAuth: requiresAuth),
        body: body != null ? json.encode(body) : null,
      ).timeout(Duration(milliseconds: ApiConfig.connectTimeout));

      return _handleResponse<T>(response, fromJson);
    } catch (e) {
      return ApiResponse(
        code: 'NETWORK_ERROR',
        message: '网络请求失败：${e.toString()}',
        data: null,
        success: false,
      );
    }
  }

  /// PUT 请求
  Future<ApiResponse<T>> put<T>(
    String endpoint, {
    Map<String, dynamic>? body,
    bool requiresAuth = false,
    T Function(Map<String, dynamic>)? fromJson,
  }) async {
    try {
      final response = await http.put(
        Uri.parse('$_baseUrl$endpoint'),
        headers: _getHeaders(requiresAuth: requiresAuth),
        body: body != null ? json.encode(body) : null,
      ).timeout(Duration(milliseconds: ApiConfig.connectTimeout));

      return _handleResponse<T>(response, fromJson);
    } catch (e) {
      return ApiResponse(
        code: 'NETWORK_ERROR',
        message: '网络请求失败：${e.toString()}',
        data: null,
        success: false,
      );
    }
  }

  /// DELETE 请求
  Future<ApiResponse<T>> delete<T>(
    String endpoint, {
    bool requiresAuth = false,
    T Function(Map<String, dynamic>)? fromJson,
  }) async {
    try {
      final response = await http.delete(
        Uri.parse('$_baseUrl$endpoint'),
        headers: _getHeaders(requiresAuth: requiresAuth),
      ).timeout(Duration(milliseconds: ApiConfig.connectTimeout));

      return _handleResponse<T>(response, fromJson);
    } catch (e) {
      return ApiResponse(
        code: 'NETWORK_ERROR',
        message: '网络请求失败：${e.toString()}',
        data: null,
        success: false,
      );
    }
  }

  /// 处理响应
  ApiResponse<T> _handleResponse<T>(
    http.Response response,
    T Function(Map<String, dynamic>)? fromJson,
  ) {
    try {
      final json = jsonDecode(response.body) as Map<String, dynamic>;
      return ApiResponse<T>.fromJson(json, fromJson);
    } catch (e) {
      return ApiResponse(
        code: 'PARSE_ERROR',
        message: '响应解析失败：${e.toString()}',
        data: null,
        success: false,
      );
    }
  }
}
```

### 更新认证服务

**文件**: `lib/services/auth_service.dart`

在原有基础上添加后端 API 集成：

```dart
import 'dart:convert';
import 'package:shared_preferences/shared_preferences.dart';
import '../models/user_model.dart';
import 'api_client.dart';

/// 认证服务 - 处理用户登录、注册、登出
class AuthService {
  static const String _userKey = 'current_user';
  static const String _tokenKey = 'auth_token';
  static const String _refreshTokenKey = 'refresh_token';
  static const String _isLoggedInKey = 'is_logged_in';

  static final ApiClient _apiClient = ApiClient();

  /// 获取当前登录用户
  static Future<UserModel?> getCurrentUser() async {
    final prefs = await SharedPreferences.getInstance();
    final userJson = prefs.getString(_userKey);
    if (userJson == null) return null;

    try {
      final Map<String, dynamic> data = json.decode(userJson);
      return UserModel.fromJson(data);
    } catch (e) {
      return null;
    }
  }

  /// 获取 Access Token
  static Future<String?> getToken() async {
    final prefs = await SharedPreferences.getInstance();
    return prefs.getString(_tokenKey);
  }

  /// 检查是否已登录
  static Future<bool> isLoggedIn() async {
    final prefs = await SharedPreferences.getInstance();
    return prefs.getBool(_isLoggedInKey) ?? false;
  }

  /// 登录 - 连接后端 API
  static Future<Map<String, dynamic>> login({
    required String email,
    required String password,
  }) async {
    try {
      final response = await _apiClient.post(
        '/auth/login',
        body: {
          'email': email,
          'password': password,
        },
      );

      if (response.success && response.data != null) {
        final userData = response.data!;
        final user = UserModel.fromJson(userData['user']);
        final tokens = userData['tokens'];

        // 保存用户信息
        await _saveUser(user);
        // 保存 Token
        await _saveToken(tokens['accessToken'], tokens['refreshToken']);
        // 设置登录状态
        await _setLoggedIn(true);

        return {'success': true, 'user': user};
      } else {
        return {'success': false, 'error': response.message ?? '登录失败'};
      }
    } catch (e) {
      return {'success': false, 'error': '网络错误：${e.toString()}'};
    }
  }

  /// 注册 - 连接后端 API
  static Future<Map<String, dynamic>> register({
    required String username,
    required String email,
    required String password,
  }) async {
    try {
      final response = await _apiClient.post(
        '/auth/register',
        body: {
          'username': username,
          'email': email,
          'password': password,
        },
      );

      if (response.success) {
        // 注册成功后自动登录
        return await login(email: email, password: password);
      } else {
        return {'success': false, 'error': response.message ?? '注册失败'};
      }
    } catch (e) {
      return {'success': false, 'error': '网络错误：${e.toString()}'};
    }
  }

  /// 登出
  static Future<void> logout() async {
    try {
      // 调用后端登出接口
      await _apiClient.post('/auth/logout', requiresAuth: true);
    } catch (e) {
      // 忽略登出错误
    } finally {
      // 清除本地数据
      final prefs = await SharedPreferences.getInstance();
      await prefs.remove(_userKey);
      await prefs.remove(_tokenKey);
      await prefs.remove(_refreshTokenKey);
      await prefs.setBool(_isLoggedInKey, false);
    }
  }

  /// 刷新 Token
  static Future<bool> refreshToken() async {
    try {
      final prefs = await SharedPreferences.getInstance();
      final refreshToken = prefs.getString(_refreshTokenKey);

      if (refreshToken == null) return false;

      final response = await _apiClient.post(
        '/auth/refresh-token',
        body: {'refreshToken': refreshToken},
      );

      if (response.success && response.data != null) {
        final tokens = response.data!['tokens'];
        await _saveToken(tokens['accessToken'], tokens['refreshToken']);
        return true;
      }

      return false;
    } catch (e) {
      return false;
    }
  }

  /// 保存用户到本地存储
  static Future<void> _saveUser(UserModel user) async {
    final prefs = await SharedPreferences.getInstance();
    await prefs.setString(_userKey, json.encode(user.toJson()));
  }

  /// 保存 Token
  static Future<void> _saveToken(String accessToken, String refreshToken) async {
    final prefs = await SharedPreferences.getInstance();
    await prefs.setString(_tokenKey, accessToken);
    await prefs.setString(_refreshTokenKey, refreshToken);
    
    // 更新 API Client 的 Token
    _apiClient.setAccessToken(accessToken);
  }

  /// 设置登录状态
  static Future<void> _setLoggedIn(bool value) async {
    final prefs = await SharedPreferences.getInstance();
    await prefs.setBool(_isLoggedInKey, value);
  }

  /// 更新当前用户信息
  static Future<void> updateUser(UserModel user) async {
    await _saveUser(user);
  }
}
```

### 创建商品服务

**文件**: `lib/services/product_service.dart`

```dart
import '../models/product_model.dart';
import 'api_client.dart';

/// 商品服务 - 处理商品相关 API
class ProductService {
  static final ApiClient _apiClient = ApiClient();

  /// 获取商品列表
  static Future<List<Product>> getProducts({
    int page = 1,
    int limit = 20,
    int? categoryId,
    String? keyword,
    bool? isOnSale,
  }) async {
    final response = await _apiClient.get(
      '/products',
      queryParams: {
        'page': page.toString(),
        'limit': limit.toString(),
        if (categoryId != null) 'categoryId': categoryId.toString(),
        if (keyword != null) 'keyword': keyword,
        if (isOnSale != null) 'isOnSale': isOnSale.toString(),
      },
    );

    if (response.success && response.data != null) {
      final productsData = response.data!['products'] as List;
      return productsData
          .map((item) => Product.fromJson(item as Map<String, dynamic>))
          .toList();
    }

    return [];
  }

  /// 获取商品详情
  static Future<Product?> getProductDetail(int productId) async {
    final response = await _apiClient.get('/products/$productId');

    if (response.success && response.data != null) {
      return Product.fromJson(response.data!);
    }

    return null;
  }

  /// 获取商品分类
  static Future<List<Map<String, dynamic>>> getCategories() async {
    final response = await _apiClient.get('/products/categories');

    if (response.success && response.data != null) {
      return response.data! as List<Map<String, dynamic>>;
    }

    return [];
  }
}
```

### 创建设备服务

**文件**: `lib/services/device_service.dart`

```dart
import '../models/device_model.dart';
import 'api_client.dart';

/// 设备服务 - 处理设备相关 API
class DeviceService {
  static final ApiClient _apiClient = ApiClient();

  /// 获取设备列表
  static Future<List<DeviceModel>> getDevices() async {
    final response = await _apiClient.get('/devices', requiresAuth: true);

    if (response.success && response.data != null) {
      final devicesData = response.data! as List;
      return devicesData
          .map((item) => DeviceModel.fromJson(item as Map<String, dynamic>))
          .toList();
    }

    return [];
  }

  /// 获取设备详情
  static Future<DeviceModel?> getDeviceDetail(String deviceId) async {
    final response = await _apiClient.get('/devices/$deviceId', requiresAuth: true);

    if (response.success && response.data != null) {
      return DeviceModel.fromJson(response.data!);
    }

    return null;
  }

  /// 绑定设备
  static Future<bool> bindDevice(String deviceId, String bindCode) async {
    final response = await _apiClient.post(
      '/devices/$deviceId/bind',
      body: {'bindCode': bindCode},
      requiresAuth: true,
    );

    return response.success;
  }

  /// 控制设备
  static Future<bool> controlDevice(
    String deviceId,
    String action,
    Map<String, dynamic> params,
  ) async {
    final response = await _apiClient.post(
      '/devices/$deviceId/control',
      body: {
        'action': action,
        'params': params,
      },
      requiresAuth: true,
    );

    return response.success;
  }
}
```

---

## 联调测试

### 测试清单

#### 1. 认证接口测试 ✅

```bash
# 1. 注册
curl -X POST http://localhost:3005/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "email": "test@lynshae.com",
    "password": "123456"
  }'

# 2. 登录
curl -X POST http://localhost:3005/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@lynshae.com",
    "password": "123456"
  }'

# 3. 获取当前用户
curl http://localhost:3005/api/v1/auth/me \
  -H "Authorization: Bearer <access_token>"
```

**Flutter 测试代码**：
```dart
// 在 Flutter 应用中测试
final result = await AuthService.login(
  email: 'test@lynshae.com',
  password: '123456',
);
print('登录结果：${result['success']}');
```

#### 2. 商品接口测试 ✅

```bash
# 1. 获取商品列表
curl http://localhost:3005/api/v1/products?limit=2

# 2. 获取商品详情
curl http://localhost:3005/api/v1/products/1

# 3. 获取商品分类
curl http://localhost:3005/api/v1/products/categories
```

**Flutter 测试代码**：
```dart
// 在 Flutter 应用中测试
final products = await ProductService.getProducts(limit: 10);
print('商品数量：${products.length}');
```

#### 3. 设备接口测试 ✅

```bash
# 1. 获取设备列表
curl http://localhost:3005/api/v1/devices \
  -H "Authorization: Bearer <access_token>"

# 2. 获取设备详情
curl http://localhost:3005/api/v1/devices/LS-DOG-2026-001 \
  -H "Authorization: Bearer <access_token>"
```

**Flutter 测试代码**：
```dart
// 在 Flutter 应用中测试
final devices = await DeviceService.getDevices();
print('设备数量：${devices.length}');
```

### 测试脚本

创建自动化测试脚本：

**文件**: `backend/tests/integration_test.js`

```javascript
const http = require('http');

const BASE_URL = 'http://localhost:3005/api/v1';

function request(method, path, body = null, token = null) {
  return new Promise((resolve, reject) => {
    const url = new URL(path, BASE_URL);
    const options = {
      hostname: url.hostname,
      port: url.port,
      path: url.pathname + url.search,
      method,
      headers: {
        'Content-Type': 'application/json',
      },
    };

    if (token) {
      options.headers['Authorization'] = `Bearer ${token}`;
    }

    const req = http.request(options, (res) => {
      let data = '';
      res.on('data', chunk => data += chunk);
      res.on('end', () => {
        try {
          resolve({
            status: res.statusCode,
            data: JSON.parse(data),
          });
        } catch (e) {
          resolve({
            status: res.statusCode,
            data: data,
          });
        }
      });
    });

    req.on('error', reject);
    if (body) req.write(JSON.stringify(body));
    req.end();
  });
}

async function runTests() {
  console.log('🧪 开始集成测试...\n');

  // 1. 健康检查
  console.log('1️⃣  健康检查');
  const health = await request('GET', '/health');
  console.log('   状态:', health.status === 200 ? '✅' : '❌');
  console.log('   响应:', health.data);
  console.log();

  // 2. 注册
  console.log('2️⃣  用户注册');
  const register = await request('POST', '/auth/register', {
    username: 'testuser',
    email: 'test@lynshae.com',
    password: '123456',
  });
  console.log('   状态:', register.status === 200 ? '✅' : '❌');
  console.log();

  // 3. 登录
  console.log('3️⃣  用户登录');
  const login = await request('POST', '/auth/login', {
    email: 'test@lynshae.com',
    password: '123456',
  });
  console.log('   状态:', login.status === 200 ? '✅' : '❌');
  const token = login.data.data?.tokens?.accessToken;
  console.log();

  // 4. 获取商品列表
  console.log('4️⃣  获取商品列表');
  const products = await request('GET', '/products?limit=2');
  console.log('   状态:', products.status === 200 ? '✅' : '❌');
  console.log('   商品数量:', products.data.data?.products?.length || 0);
  console.log();

  // 5. 获取设备列表
  console.log('5️⃣  获取设备列表');
  const devices = await request('GET', '/devices', null, token);
  console.log('   状态:', devices.status === 200 ? '✅' : '❌');
  console.log('   设备数量:', devices.data.data?.length || 0);
  console.log();

  console.log('✅ 测试完成！');
}

runTests().catch(console.error);
```

运行测试：
```bash
cd backend
node tests/integration_test.js
```

---

## 常见问题

### 1. 跨域问题 (CORS)

**现象**: Flutter Web 访问后端时报 CORS 错误

**解决方案**: 在后端 `src/server.js` 中配置 CORS：

```javascript
const cors = require('cors');

app.use(cors({
  origin: ['http://localhost:3000', 'http://localhost:8080'], // Flutter Web 端口
  credentials: true,
}));
```

### 2. Android 明文 HTTP 限制

**现象**: Android 9+ 不允许 HTTP 请求

**解决方案**: 在 `android/app/src/main/AndroidManifest.xml` 中添加：

```xml
<application
    android:usesCleartextTraffic="true"
    ...>
```

### 3. iOS ATS 限制

**现象**: iOS 不允许 HTTP 请求

**解决方案**: 在 `ios/Runner/Info.plist` 中添加：

```xml
<key>NSAppTransportSecurity</key>
<dict>
    <key>NSAllowsArbitraryLoads</key>
    <true/>
</dict>
```

### 4. Token 过期处理

**解决方案**: 在 API Client 中添加 Token 刷新逻辑：

```dart
Future<ApiResponse<T>> _retryWithRefreshToken<T>(
  Future<ApiResponse<T>> Function() request,
) async {
  final refreshed = await AuthService.refreshToken();
  if (refreshed) {
    final newToken = await AuthService.getToken();
    _apiClient.setAccessToken(newToken);
    return await request();
  }
  // Token 刷新失败，跳转到登录页
  await AuthService.logout();
  return ApiResponse(
    code: 'UNAUTHORIZED',
    message: '登录已过期',
    success: false,
    data: null,
  );
}
```

### 5. 本地开发网络问题

**现象**: Flutter 模拟器无法访问 localhost

**解决方案**:

- **Android 模拟器**: 使用 `10.0.2.2` 代替 `localhost`
- **iOS 模拟器**: 使用 `localhost` 即可
- **真机调试**: 使用局域网 IP，如 `192.168.1.100`

修改配置：
```dart
class ApiConfig {
  static const String devBaseUrl = 'http://10.0.2.2:3005/api/v1'; // Android
  // 或
  static const String devBaseUrl = 'http://192.168.1.100:3005/api/v1'; // 真机
}
```

---

## 下一步

- [ ] 完善订单接口联调
- [ ] 实现 WebSocket 实时通信
- [ ] 添加文件上传功能测试
- [ ] 性能优化与压力测试

---

*最后更新：2026-03-14*
*版本：1.0.0*
